# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Actions cache
.cache/

# Local environment secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Test / coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Ephemeral run artifacts — timestamped briefings generated each agent run.
# reports/index.html and reports/latest.md ARE committed (published to Pages).
reports/briefing_*.md

# Ephemeral state — reconstructible on clean clone via bootstrap_seen_from_reports().
# Durable state (finding_shelf, ioc_ledger, feed_health, ledger.jsonl) stays committed.
state/seen_hashes.json
state/last_run_cards.json
# briefing_*.jsonl are committed so 7-day history, weekly metrics, and CVE timeline work.

# Internal development roadmap — not for public distribution.
upgrades.md
